{
  "name": "cloudfront-invalidator",
  "version": "1.0.0",
  "description": "Lambda function to invalidate CloudFront cache on Amplify build completion",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@aws-sdk/client-cloudfront": "^3.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
